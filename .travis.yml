sudo: false
language: python
python:
  - '2.6'
  - '2.7'
  - '3.4'
  - '3.5'
install:
  - pip install python-coveralls
  - pip install coverage
  - pip install nose
  - pip install future

script:
  - python setup.py install
  - nosetests --with-coverage

after_success:
  - coveralls
